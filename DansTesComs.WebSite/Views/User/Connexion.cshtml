@model User

@{
    ViewBag.Title = "Connexion";
}

<h1>@GeneralRessources.SeConnecter</h1>
<hr class="empty-hr row"/>
@if (!string.IsNullOrEmpty(ViewBag.Creation))
{
    <p>@ViewBag.Creation</p>
}
<div class="col-md-offset-1 col-md-10">
    @Html.Partial("ConnexionFormPartial");
</div>